# Rust
/target
flasher/target
**/*.rs.bk
Cargo.lock.orig

# OS
.DS_Store

# ktflash operation journals (per-flash, user-local; never commit)
ktflash-*.journal.json

# local RE artifacts / vendor blobs (never commit vendor firmware or the .exe)
*.exe
*.bin
*.rzdb
ghidra_proj/

# build outputs
macos/native/e1_hid_setreport
macos/native/e3_interface_seize
macos/native/ktmac/.build/
tools/vmatrix/vmatrix
